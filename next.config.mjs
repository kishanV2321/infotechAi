/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', 'ik.imagekit.io', 'randomuser.me']
    },
};


export default nextConfig;
